# Quest build baseline

This file marks the first Android/Quest build validation pass from the `miami` branch.

Initial goal: confirm the existing Android launcher can still produce installable debug and release APK artifacts before any OpenXR or VR changes are introduced.

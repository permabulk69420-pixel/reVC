#if defined(ANDROID)

// Intentionally inactive for the normal-camera stereo baseline.
// The baseline leaves reVC/RenderWare's original camera matrix untouched so
// hardware testing can isolate per-eye separation and presentation aspect from
// every prior head-pose, first-person and camera-basis experiment.

#endif

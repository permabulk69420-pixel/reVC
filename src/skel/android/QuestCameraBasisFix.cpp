#if defined(ANDROID)

// Intentionally inactive for the zero-transform stereo-screen baseline.
// Vice City's internal camera matrix and RenderWare camera frame must remain
// byte-for-byte on their ordinary game path while the compositor presentation
// and second-eye replay are diagnosed independently.

#endif

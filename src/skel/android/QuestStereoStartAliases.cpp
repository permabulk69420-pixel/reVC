#if defined(ANDROID)

// The old anonymous-namespace start aliases belonged to the post-culling replay
// prototype. QuestOpenXRGameStereo.cpp now calls the linker real symbols
// directly after rebuilding the right-eye render list.

#endif
